#include "findDriverByIdUseCase.h"

void findDriverByIdUseCase(const char *id)
{
  // A função abaixo deverá estar presente dentro de driverRepository
  // Driver driver = findDriverById(id);

  // print(driver);
}
