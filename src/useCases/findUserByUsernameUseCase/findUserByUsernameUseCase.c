#include "findUserByUsernameUseCase.h"

void findUserByUsernameUseCase(const char *username)
{
  // A função abaixo deverá estar presente dentro de userRepository
  // User user = findUserByUsername(username);

  // print(user);
}