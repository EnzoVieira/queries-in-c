#ifndef VALIDATEFUNCS_H
#define VALIDATEFUNCS_H

int validateDate (char* date);
int validateCarClass(char *carClass);
int validateStatus(char *status);
int validatePositiveInt (char* number);
int validatePositiveFloat (char* number);


#endif
